name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: node:14.15.0-alpine3.12
    steps:
      - uses: actions/checkout@v2
      - name: Checking PIO
        runs: |
          platformio pkg update