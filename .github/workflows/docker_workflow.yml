name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: petewall/platformio:6.1.4
    steps:
      - uses: actions/checkout@v2

      - name: Checking PIO
        run: |
          # pio upgrade
          pio run
