name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: sglahn/platformio-core:latest
    steps:
      - uses: actions/checkout@v2

      - name: Checking PIO
        run: |
          pio upgrade
          pio run
